using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SomeScrr : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Let's play!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
